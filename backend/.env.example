# =============================================================================
# REG-01 SYSTEM CONFIGURATION (.env)
# =============================================================================

# -----------------------------------------------------------------------------
# 1. LLM PROVIDER SETTINGS
# -----------------------------------------------------------------------------
# ตัวเลือก: gemini, openai (รวมถึง Groq), local
LLM_PROVIDER=openai

# Google Gemini Settings
GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_MODEL_NAME=gemini-3-flash-preview

# OpenAI / Groq / External Provider Settings
# ในตัวอย่างนี้ใช้ Groq Cloud (llama-3.3-70b)
OPENAI_API_KEY=gsk_ZWgxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.groq.com/openai/v1
OPENAI_MODEL_NAME=llama-3.3-70b-versatile

# Local Model Settings (Ollama / vLLM)
LOCAL_API_KEY=ollama
LOCAL_MODEL_NAME=iapp/chinda-qwen3-4b
LOCAL_BASE_URL=http://localhost:11434/v1

# -----------------------------------------------------------------------------
# 2. SERVER & NETWORK CONFIGURATION
# -----------------------------------------------------------------------------
PORT=5000
HOST=0.0.0.0
ALLOWED_ORIGINS=*

# --- CLOUDFLARE CONFIG ---
# หากตั้งเป็น true จะใช้ Quick Tunnel (ได้ลิงค์สุ่ม ไม่ต้องมีโดเมน)
USE_QUICK_TUNNEL=true
# หากใช้ Named Tunnel ให้ใส่ชื่อด้านล่าง (ต้องตั้ง USE_QUICK_TUNNEL=false)
CLOUDFLARE_TUNNEL_NAME=reg01-tunnel

# -----------------------------------------------------------------------------
# 3. FACEBOOK MESSENGER WEBHOOK SETTINGS
# -----------------------------------------------------------------------------
# รหัส Verify Token ที่ต้องไปใส่ใน Facebook Developer Console (Webhook Setup)
FB_VERIFY_TOKEN=verify123

# รหัส Access Token จากหน้าเพจ Facebook
FB_PAGE_ACCESS_TOKEN=EAAVDQHeZC27xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# รหัส App Secret จากหน้าตั้งค่า App ใน Facebook Developer
FB_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# 4. RAG & KNOWLEDGE BASE ENGINE
# -----------------------------------------------------------------------------
# เปิดใช้งานระบบ RAG (Retrieval Augmented Generation)
USE_RETRIEVAL_ENGINE=true

# โฟลเดอร์เก็บ PDF (ถ้าไม่ระบุจะใช้ค่า Default ใน backend/app/static/docs)
# PDF_INPUT_FOLDER=./static/docs

# โฟลเดอร์เก็บ TXT ที่ประมวลผลแล้ว
# PDF_QUICK_USE_FOLDER=./static/quick_use

# -----------------------------------------------------------------------------
# 5. SECURITY, PERFORMANCE & AUDIT
# -----------------------------------------------------------------------------
# [Phase 1] จำกัดการเรียก LLM พร้อมกันเพื่อประหยัด Resource
MAX_CONCURRENT_LLM_CALLS=10

# [Phase 2] คีย์สำหรับตรวจสอบ SSO / JWT
AUTH_SECRET_KEY=your-university-sso-secret

# [Phase 3] คีย์สำหรับเข้าดูหน้า Admin Analytics (/api/admin/stats)
ADMIN_TOKEN=super-secret-key

# โฟลเดอร์เก็บประวัติแชทแยกตาม Session ID
# SESSION_DIR=../memory/session_storage

# =============================================================================
# END OF CONFIGURATION
# =============================================================================